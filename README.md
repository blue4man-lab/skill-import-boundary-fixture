# Skill import boundary fixture

Synthetic, non-sensitive text fixtures for public repository import verification.

- `skills/import-max-boundary`: exactly 256 files and 31,457,280 bytes (30 MiB), including SKILL.md.
- `skills/import-small-selection`: one small SKILL.md within the same repository.
- `manifest.json`: expected sizes and hashes, outside the selected skill directory.

There are no credentials, user data, or executable scripts in this repository.
